<%# Where private user information can be stored %>
<br>
<%= @user.owned_projects.count %> project boards created
<%= @user.tasks.count %> tasks created
<%= Task.completed_tasks(@user) %> tasks completed
<%= link_to "Edit my account information", edit_user_path(@user) %><br>
<%= link_to "Delete my account", user_path(@user), method: :delete, data: {confirm: 'Are you sure?'} %>