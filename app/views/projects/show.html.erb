<br>
<h2 style="text-align: center"><%= @project.title %></h2>

<%= render partial: 'task', locals: {type: "Future"} %>

<%= render partial: 'task', locals: {type: "Current"} %>

<%= render partial: 'task', locals: {type: "Complete"} %>

<div style="width: 100%; float: left">
<% if current_user %>
  <%= button_to "Add Task", new_project_task_path(project_id: @project.id), method: :get %>
<% end %>
  <br><br><br>
</div>