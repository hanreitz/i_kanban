<h4><%= @task.description %>, added by <%= link_to @task.user.username, user_path(@task.user) %></h4><br><br>

Project Board: <%= @task.project.title %>, created by <%= link_to @task.project.owner_object.username, user_path(@task.project.owner_object) %><br><br>

Due <%= @task.due_date %><br><br>

<%= @task.priority %> Priority<br><br>

Stage in Process: <%= @task.category %> Task<br><br>

<%= button_to "Edit Task", edit_task_path(@task), method: :get, form: {style: 'display:inline-block;'} %>
<%= button_to "Delete Task", task_path(@task), method: :delete, form: {style: 'display:inline-block;'}, data: {confirm: 'Are you sure?'} %>
<br><br><br>